apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow-vicitm-555
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - name: Say hello-vicimt
        run: |
          echo "hello world"
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
  vicitm-jon555:
    environment: "5555"
    steps:
      - name: te
        uses: teast
